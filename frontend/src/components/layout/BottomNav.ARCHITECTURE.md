# BottomNav Visual Architecture

## Component Hierarchy

```
App (BrowserRouter)
│
├── Layout Component
│   │
│   ├── Header (Desktop Only)
│   │   └── className="hidden md:block"
│   │
│   ├── <main> Content Area
│   │   ├── className="pt-0 md:pt-16 pb-20 md:pb-0"
│   │   │
│   │   └── <Outlet> (React Router)
│   │       ├── /dashboard → DashboardPage
│   │       ├── /archive → ArchivePage
│   │       ├── /receipts/new → NewReceiptPage
│   │       └── /profile → ProfilePage
│   │
│   └── BottomNav (Mobile Only)
│       └── className="md:hidden"
│
└── Modals (Optional)
    └── ExportModal (triggered by BottomNav)
```

---

## Screen Layout (Mobile)

```
┌─────────────────────────────────────┐
│                                     │ ← Header (hidden on mobile)
├─────────────────────────────────────┤
│                                     │
│                                     │
│                                     │
│         Main Content Area           │
│       (pb-20 for BottomNav)         │
│                                     │
│                                     │
│                                     │
├─────────────────────────────────────┤
│                  ⊕                  │ ← Center button (elevated)
│                                     │
│ 🏠    📁         Export    👤       │ ← BottomNav (fixed)
│בית   ארכיון              פרופיל    │
└─────────────────────────────────────┘
```

---

## Screen Layout (Desktop)

```
┌─────────────────────────────────────┐
│  Logo    Dashboard Archive Profile  │ ← Header (visible)
├─────────────────────────────────────┤
│                                     │
│                                     │
│                                     │
│         Main Content Area           │
│          (no pb-20 here)            │
│                                     │
│                                     │
│                                     │
│                                     │
└─────────────────────────────────────┘
                                       ← BottomNav (hidden)
```

---

## Component Structure (BottomNav.tsx)

```
<nav> (BottomNav Container)
│
└── <div> (Navigation Items Container)
    │
    ├── Item 1: Home
    │   ├── <Link to="/dashboard">
    │   ├── Icon: <Home />
    │   └── Label: "בית"
    │
    ├── Item 2: Archive
    │   ├── <Link to="/archive">
    │   ├── Icon: <FolderOpen />
    │   └── Label: "ארכיון"
    │
    ├── Item 3: Add Receipt (CENTER - ELEVATED)
    │   ├── <Link to="/receipts/new">
    │   ├── className="absolute -top-5"
    │   ├── Icon: <Plus /> (28px, large)
    │   └── No Label
    │
    ├── Item 4: Export
    │   ├── <button onClick={onExportClick}>
    │   ├── Icon: <Download />
    │   └── Label: "ייצוא"
    │
    └── Item 5: Profile
        ├── <Link to="/profile">
        ├── Icon: <User />
        └── Label: "פרופיל"
```

---

## State Flow Diagram

```
User Clicks Nav Item
        ↓
┌───────────────────┐
│  Is Center Button?│
└───────────────────┘
    Yes ↓        ↓ No
        ↓        ↓
        ↓   ┌────────────────┐
        ↓   │ Is Link or Btn?│
        ↓   └────────────────┘
        ↓     Link ↓    ↓ Button
        ↓          ↓    ↓
        ↓    React Router  onClick Callback
        ↓    Navigate      (e.g., Export)
        ↓          ↓    ↓
        └──────────┴────┘
                ↓
        useLocation() Hook
                ↓
        Match Current Route?
                ↓
         Yes ↓    ↓ No
             ↓    ↓
    Active State  Inactive State
    (Blue, 110%)  (Gray, 100%)
```

---

## Responsive Behavior Flow

```
Window Width Check
        ↓
┌──────────────────┐
│   width < 768px  │
│   (Mobile/Tablet)│
└──────────────────┘
        Yes ↓
            ↓
    ┌───────────────────┐
    │  Show BottomNav   │
    │  className=""     │
    └───────────────────┘
            ↓
    ┌───────────────────┐
    │  Hide Header      │
    │  md:block (false) │
    └───────────────────┘
            ↓
    ┌───────────────────┐
    │ Main padding:     │
    │ pb-20 (active)    │
    └───────────────────┘

        No ↓
            ↓
    ┌───────────────────┐
    │  Hide BottomNav   │
    │  md:hidden (true) │
    └───────────────────┘
            ↓
    ┌───────────────────┐
    │  Show Header      │
    │  md:block (true)  │
    └───────────────────┘
            ↓
    ┌───────────────────┐
    │ Main padding:     │
    │ pb-0 (active)     │
    └───────────────────┘
```

---

## Navigation Item Rendering Logic

```
navItems.map((item) => {
    ↓
┌─────────────────┐
│ Is Center Item? │
└─────────────────┘
    Yes ↓        ↓ No
        ↓        ↓
 Elevated Button  ┌──────────────┐
 (absolute pos)   │ Has route?   │
 64x64 circle     └──────────────┘
 Gradient bg       Yes ↓    ↓ No
 -20px top              ↓    ↓
 Large icon        <Link>  <button>
 No label          Regular  onClick
                   nav item handler
                        ↓
                ┌────────────────┐
                │ Check Active   │
                │ useLocation()  │
                └────────────────┘
                        ↓
                 Active Styling:
                 - Primary color
                 - Scale 110%
                 - Semi-bold label
})
```

---

## Touch Target Sizing

```
Regular Nav Items:
┌────────────────────┐
│                    │
│   ┌──────────┐     │  Min: 48px × 48px
│   │   Icon   │     │  Actual: 56px × 56px
│   │  (24px)  │     │  (with padding)
│   └──────────┘     │
│      Label         │
│                    │
└────────────────────┘

Center Button:
     ┌────────┐
     │        │
     │   +    │      Size: 64px × 64px
     │ (28px) │      Touch: Full circle
     │        │      Elevated: -20px
     └────────┘
```

---

## Z-Index Stacking Order

```
                  ↑ Higher (Front)
                  │
    ┌─────────────────────────┐
    │ Modal/Overlay           │ z-[100]
    └─────────────────────────┘
                  │
    ┌─────────────────────────┐
    │ Dropdown Menus          │ z-[60]
    └─────────────────────────┘
                  │
    ┌─────────────────────────┐
    │ BottomNav               │ z-50 (default)
    └─────────────────────────┘
                  │
    ┌─────────────────────────┐
    │ Header (Desktop)        │ z-40
    └─────────────────────────┘
                  │
    ┌─────────────────────────┐
    │ Main Content            │ z-0 (default)
    └─────────────────────────┘
                  │
                  ↓ Lower (Back)
```

---

## Color State Transitions

```
Nav Item States:

Inactive → Hover → Active
#6B7280   #374151  #2563EB
(Gray)    (Darker) (Blue)
  ↓         ↓        ↓
100%      100%     110% (icon scale)
Normal    Normal   Semi-bold (label)

Transition: all 200ms ease
```

---

## Data Flow (Export Example)

```
User clicks Export button
        ↓
onClick event fires
        ↓
onExportClick prop callback
        ↓
Parent component (App/Layout)
        ↓
setState(isExportModalOpen = true)
        ↓
Conditional render ExportModal
        ↓
Modal appears (z-[100])
        ↓
User interacts with modal
        ↓
Modal closes
        ↓
setState(isExportModalOpen = false)
```

---

## Padding & Spacing Relationships

```
Mobile Layout:
┌─────────────────────────────────┐
│ Main Content                    │
│ padding-bottom: 80px (pb-20)    │
│                                 │
│ ┌─────┐                         │
│ │  64px (nav height)            │
│ │  +                            │
│ │  16px (comfortable gap)       │
│ │  =                            │
│ │  80px total                   │
│ └─────┘                         │
│                                 │
│ [Navigation Bar - 64px]         │
└─────────────────────────────────┘

Desktop Layout:
┌─────────────────────────────────┐
│ Header - 64px (pt-16)           │
│                                 │
│ Main Content                    │
│ padding-top: 64px               │
│ padding-bottom: 0 (no BottomNav)│
│                                 │
└─────────────────────────────────┘
```

---

## Component Communication Flow

```
                App.tsx
                   ↓
        ┌──────────┴──────────┐
        │                     │
     Header              BottomNav
        │                     │
        │                     ├→ onExportClick prop
        │                     │
        │                     ├→ useLocation() hook
        │                     │  (detects route)
        │                     │
        │                     └→ Highlights active item
        │
    Desktop Nav          Mobile Nav
    (visible ≥768px)     (visible <768px)
```

---

## Accessibility Tree

```
<nav role="navigation" aria-label="ניווט ראשי">
  │
  ├── <Link aria-label="בית" aria-current="page"> (if active)
  │     └── Home Icon + Text
  │
  ├── <Link aria-label="ארכיון">
  │     └── Archive Icon + Text
  │
  ├── <Link aria-label="הוסף קבלה חדשה">
  │     └── Plus Icon (elevated button)
  │
  ├── <button aria-label="ייצוא" role="button">
  │     └── Download Icon + Text
  │
  └── <Link aria-label="פרופיל">
        └── User Icon + Text
```

---

## File Dependencies

```
BottomNav.tsx
    ↓
    ├── react (useState, useEffect)
    ├── react-router-dom (Link, useLocation)
    ├── lucide-react (Icons)
    ├── @/utils/formatters (cn utility)
    └── Tailwind CSS (styling)
        ↓
        └── tailwind.config.js
            └── Design system colors/spacing
```

---

## Integration Points

```
Your App
    ↓
┌─────────────────────────────────┐
│ 1. Import BottomNav             │
│    from '@/components/layout'   │
└─────────────────────────────────┘
    ↓
┌─────────────────────────────────┐
│ 2. Add to Layout/App.tsx        │
│    <BottomNav />                │
└─────────────────────────────────┘
    ↓
┌─────────────────────────────────┐
│ 3. Add padding to main          │
│    className="pb-20 md:pb-0"    │
└─────────────────────────────────┘
    ↓
┌─────────────────────────────────┐
│ 4. Create route handlers        │
│    /dashboard, /archive, etc.   │
└─────────────────────────────────┘
    ↓
┌─────────────────────────────────┐
│ 5. Add export callback          │
│    onExportClick={handler}      │
└─────────────────────────────────┘
    ↓
   ✅ Ready to use!
```

---

## Performance Optimization Flow

```
Component Mount
    ↓
useLocation() subscribes to router
    ↓
Only re-renders on route change
    ↓
CSS transitions (hardware accelerated)
    ↓
touch-manipulation for better mobile perf
    ↓
No expensive computations
    ↓
Memoization not needed (already optimized)
```

---

## Design System Alignment

```
Tik-Tax Design System
        ↓
┌─────────────────────────────────┐
│ Colors                          │
│ - Primary: #2563EB ✅           │
│ - Gray Scale ✅                 │
│ - Status Colors (future) ✅     │
└─────────────────────────────────┘
        ↓
┌─────────────────────────────────┐
│ Typography                      │
│ - Font: Inter/Figtree ✅        │
│ - Sizes: 12px labels ✅         │
│ - Weights: 500/600 ✅           │
└─────────────────────────────────┘
        ↓
┌─────────────────────────────────┐
│ Spacing (8-point grid)          │
│ - Height: 64px (8×8) ✅         │
│ - Gaps: 4px, 8px ✅             │
│ - Padding: 12px, 16px ✅        │
└─────────────────────────────────┘
        ↓
┌─────────────────────────────────┐
│ Shadows & Elevation             │
│ - Level 1: Nav bar ✅           │
│ - Level 2: Center button ✅     │
└─────────────────────────────────┘
        ↓
┌─────────────────────────────────┐
│ Transitions                     │
│ - Duration: 200ms ✅            │
│ - Easing: ease ✅               │
└─────────────────────────────────┘
        ↓
   ✅ 100% Compliant
```

---

**Visual Architecture Guide**  
**Component**: BottomNav  
**Version**: 1.0  
**Project**: Tik-Tax Platform
